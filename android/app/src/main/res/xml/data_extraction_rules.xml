<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ backup/transfer policy. The app does not participate in cloud
    backup or device-to-device transfer: contact data is sensitive and the
    encrypted session token would be undecryptable on another device anyway
    (the Keystore master key never leaves this device).
-->
<data-extraction-rules>
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
