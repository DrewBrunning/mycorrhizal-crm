<?xml version="1.0" encoding="utf-8"?>
<!--
    TLS trust model (ticket §9.5).

    Production: system CAs only, cleartext forbidden. Self-hosted servers with
    self-signed certs are handled via the KeyChain import flow (installCustomCertificate),
    never by disabling TLS verification.

    Debug builds additionally trust user-installed CAs (Settings → Security →
    Install certificate) so a developer's self-signed dev cert works in the
    debug APK without shipping any trust override to production.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
